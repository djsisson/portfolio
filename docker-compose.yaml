version: "1"

services:
  app:
    container_name: app
    tty: true
    build:
      context: .
      dockerfile: dockerfile
      network: Host
    env_file:
      - ./.env
    expose:
      - 3001
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - WATCHPACK_POLLING=true
    networks:
      - dcgk88c
    volumes:
      - .:/app
    restart: unless-stopped

networks:
  bgw80w0:
    external: true
