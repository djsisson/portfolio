version: "1"

services:
  app:
    container_name: app
    tty: true
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost"]
      interval: 1m30s
      timeout: 30s
      retries: 5
      start_period: 30s
    build:
      context: .
      dockerfile: dockerfile
      network: host
    env_file:
      - ./.env
    expose:
      - 3001
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - WATCHPACK_POLLING=true
    networks:
      - bgw80w0
    restart: unless-stopped

networks:
  bgw80w0:
    external: true
